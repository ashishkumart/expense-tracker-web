name: expense_tracker_web
description: A responsive Flutter Web expense tracker.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  flutter_colorpicker: ^1.1.0
  cupertino_icons: ^1.0.8
  http: ^1.3.0
  intl: ^0.20.2
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
